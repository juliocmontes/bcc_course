#include <iostream>

using namespace std;

int main()
{
    int counter, multiplier, number, result;

    cout << "Enter two numbers: " << endl;
    cin >> multiplier >> number;

    for (counter = 0, counter <= multiplier, counter++)
    {
        result =
    }

    return 0;
}
